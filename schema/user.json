{
    "$schema": "http://json-schema.org/draft-04/schema#",
    "title": "User",
    "description": "Defines a user within the organization.",
    "id": "https://bazer.io/schema/user",
    "type": "object",
    "properties": {
        "id": {
            "title": "User ID",
            "description": "A unique identifier for the user.",
            "type": "string",
            "examples": ["usr_a9b8c7d6e5f4"]
        },
        "name": {
            "title": "User Name",
            "description": "The full name of the user.",
            "type": "string",
            "examples": ["John Doe"]
        },
        "email": {
            "title": "User Email",
            "description": "The user's email address.",
            "type": "string",
            "format": "email",
            "examples": ["john.doe@example.com"]
        },
        "teamId": {
            "title": "Team ID",
            "description": "The unique identifier of the team the user belongs to.",
            "$ref": "team.json#/properties/id"
        },
        "urls": {
            "title": "Related URLs",
            "description": "A list of URLs associated with the user, such as a personal website or profile.",
            "$ref": "defs-descriptor.json#/definitions/urls"
        }
    },
    "required": [
        "id",
        "name",
        "email"
    ]
}